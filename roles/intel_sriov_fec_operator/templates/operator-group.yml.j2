apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: vran-operators
  namespace: "{{ intel_sriov_fec_operator_namespace }}"
spec:
  targetNamespaces:
    - "{{ intel_sriov_fec_operator_namespace }}"
