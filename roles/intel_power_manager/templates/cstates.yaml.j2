apiVersion: power.intel.com/v1
kind: CStates
metadata:
  name: {{ power_node }}
  namespace: {{ intel_power_manager_namespace }}
spec:
  sharedPoolCStates:
    {{ cstates.shared }}
  exclusivePoolCStates:
    {{ cstates.profile_exclusive }}
  individualCoreCStates:
    {{ cstates.core }}
