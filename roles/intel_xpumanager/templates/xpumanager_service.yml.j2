---
apiVersion: v1
kind: Service
metadata:
  name: intel-xpumanager
  namespace: "{{ xpumanager_namespace }}"
  labels:
    app: intel-xpumanager
spec:
  type: ClusterIP
  selector:
    app: intel-xpumanager
  sessionAffinity: None
  ports:
  - name: metrics
    port: 8443
    targetPort: 8443
    protocol: TCP
